// Ticks
WB_STRATEGIC_TICK_MINUTES = 10;
WB_CARAVAN_TICK_MINUTES   = 7;
WB_TAX_TICK_MINUTES       = 12;
WB_TROOP_UPGRADE_MINUTES  = 15;

// World
WB_MAX_WARBANDS_PER_FACTION = 6;
WB_WARBAND_SIZE_RANGE = [6,12];
WB_CARAVAN_SIZE_RANGE = [4,8];
WB_BATTLE_DISTANCE    = 350;
WB_OWNERSHIP_SCORE_GAIN = 10;

// Capture
WB_CAPTURE_HEALTH_THRESHOLD = 0.25;
WB_CAPTURE_ITEM_CLASS = "Exile_Item_ZipTie";

// Arena / betting
WB_ARENA_MIN_BET = 500;
WB_ARENA_MAX_BET = 20000;

// Blackmarket
WB_BLACKMARKET_ENABLED = true;

// Performance / caps
WB_MAX_ACTIVE_GROUPS = 80;           // hard cap of concurrently simulated groups
WB_DESPAWN_DISTANCE  = 2500;         // meters from any player
WB_USE_AGENTS_FOR_CIV = true;        // use createAgent for civilian ambience

// Headless client
WB_ENABLE_HC = true;                 // if an HC connects, transfer groups to it
